export const appConfig = {
  appName: "Activity Compliance",
  apiVersion: "v1",
  defaultLocale: "en-IN",
  proofEditWindowMs: 24 * 60 * 60 * 1000,
  storageNamespace: "activity-platform"
} as const;

